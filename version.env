# Centralized version configuration for clang-tidy-automotive
# Source this file in shell scripts: source version.env

# LLVM/Clang source version used by this project
LLVM_SOURCE_VERSION="20.1.8"

# LLVM tool version (for llvm-profdata, llvm-cov, clang, etc.)
# This is the major version installed on the system
LLVM_TOOL_VERSION="20"

# Derived paths
LLVM_DIR="llvm-project-llvmorg-${LLVM_SOURCE_VERSION}"
LLVM_TARBALL="llvmorg-${LLVM_SOURCE_VERSION}.tar.gz"
LLVM_URL="https://github.com/llvm/llvm-project/archive/refs/tags/${LLVM_TARBALL}"
